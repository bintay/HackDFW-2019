<!DOCTYPE html>
<html>
<head>
   <title>Homelessn't</title>
   <meta charset='utf8' />
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link href="https://fonts.googleapis.com/css?family=Quicksand:400,700|Source+Sans+Pro" rel="stylesheet" />
   <link rel='stylesheet' type='text/css' href='/public/css/main.css' />
   <script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>
</head>
<body>
   <header>
      <div class="wrap">
         <div class="grid2-2">
            <a href="/" class='no-a'>
               <div class="col1">
                  <h1>Homelessn't 🏡</h1>
                  <h2>Getting people back on their feet since Saturday</h2>
               </div>
            </a>
            <div class="col2-2">
               <span class='right'>
               {{#if loggedOut}}
                  <a href="/login" class='right'>Login</a>
               {{else}}
                  {{#if canAdd}}
                     <a href='/manage' class='no-mobile'>Manage Libraries</a><span class='no-mobile'> / </span>
                     <a href='/add' class='no-mobile'>Add Volunteering</a><span class='no-mobile'> / </span><a href="/profile/">Profile</a> / <a href="/logout/">Log Out</a>
                  {{else}}
                     <a href="/profile/">Profile</a> / <a href="/logout/">Log Out</a>
                  {{/if}}
               {{/if}}
               </span>
            </div>
         </div>
      </div>
   </header>
   <div id="app">
      <div class="wrap">
         <h2>Information</h2>
         <div class="card">
            <p>
               Name: {{name}} <br>
               Email: {{email}}
            </p>
         </div>
         <h2>Volunteering You've Signed Up For</h2><br>
         <h3>We'll send you reminders via email.</h3>
         {{#each volunteering}}
            <div class="card">
            <div class="grid2">
               <div class="col1">
                  <span class='title'>{{ this.title }}</span><br><br>
                  <span class='grey'>{{ this.location }}</span><br>
                  <span class="grey">{{ this.dateString }}</span><br><br>
                  <span class="grey">{{ this.people }} / {{ this.needPeople }} volunteers signed up</span>
                  <br>
                  <br>
               </div>
               <div class="col2">
                  {{ this.description }}
               </div>
            </div>
         </div>
         {{/each}}
      </div>
   </div>
   <footer>
      <p class="center">&copy; Humanity, 2019 💛</p>
   </footer>
</body>
</html>